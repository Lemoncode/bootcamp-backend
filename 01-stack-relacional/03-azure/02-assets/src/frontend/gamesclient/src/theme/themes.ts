import { borders } from './default/borders';
import { colors as villaviciosaColors } from './default/colors';
import { fonts } from './default/fonts';
import { gaps } from './default/gaps';
import { margins } from './default/margins';
import { paddings } from './default/paddings';
import { widths } from './default/widths';
import { heights } from './default/heights';

export const defaultTheme = {
    id: 'RETROGAMES-THEME',
    name: 'Retro Games default theme',
    colors: villaviciosaColors,
    margins: margins,
    gaps: gaps,
    fonts: fonts,
    paddings: paddings,
    widths: widths,
    heights: heights,
    borders: borders,
};

