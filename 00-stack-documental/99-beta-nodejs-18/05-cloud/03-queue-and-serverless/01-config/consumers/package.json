{
  "name": "00-consumers",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "prestart": "sh ./create-dev-env.sh && npm run clean && npm run build:dev",
    "start": "run-p -l build:watch start:dev",
    "start:dev": "nodemon --quiet dist/index",
    "build:dev": "tsc --outDir dist",
    "build:watch": "npm run build:dev -- --watch --preserveWatchOutput",
    "clean": "rimraf dist"
  },
  "imports": {
    "#core/*.js": "./dist/core/*.js"
  },
  "dependencies": {
    "@cloudamqp/amqp-client": "^2.1.1",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "@types/node": "^18.15.3",
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.21",
    "npm-run-all": "^4.1.5",
    "rimraf": "^4.4.0",
    "typescript": "^4.9.5"
  }
}
